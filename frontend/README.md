# Inventory Frontend

React + Vite client for the Inventory API.

See the [root README](../README.md) for setup and how to run the full stack.

```powershell
npm install
npm run dev
```

API base URL: `VITE_API_URL` in `.env` (default `http://localhost:5026/api`).
